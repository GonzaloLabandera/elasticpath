<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
		 xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
		 xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>com.elasticpath.extensions</groupId>
		<artifactId>ext-sync-parent</artifactId>
		<version>0.0.0-SNAPSHOT</version>
	</parent>

	<artifactId>ext-sync-webapp</artifactId>
	<packaging>war</packaging>
	<name>DST Message Listener Server Webapp</name>

	<dependencies>
		<dependency>
			<groupId>com.elasticpath</groupId>
			<artifactId>ep-sync-message-listener-camel</artifactId>
		</dependency>
		<dependency>
			<groupId>com.elasticpath.extensions</groupId>
			<artifactId>ext-sync</artifactId>
			<version>${project.version}</version>
		</dependency>
		<dependency>
			<groupId>com.elasticpath</groupId>
			<artifactId>ep-common-web</artifactId>
		</dependency>
		<dependency>
			<groupId>org.apache.camel</groupId>
			<artifactId>camel-jms</artifactId>
		</dependency>
		<dependency>
			<groupId>com.elasticpath</groupId>
			<artifactId>ep-jms</artifactId>
		</dependency>
		<dependency>
			<groupId>com.elasticpath</groupId>
			<artifactId>ep-core-caching</artifactId>
		</dependency>
		<dependency>
			<groupId>com.elasticpath</groupId>
			<artifactId>ep-core-changeset-messaging</artifactId>
		</dependency>

		<dependency>
			<groupId>org.springframework</groupId>
			<artifactId>spring-context</artifactId>
			<scope>runtime</scope>
		</dependency>
		<dependency>
			<groupId>com.jayway.jsonpath</groupId>
			<artifactId>json-path</artifactId>
			<scope>runtime</scope>
		</dependency>
		<dependency>
			<groupId>javax.servlet</groupId>
			<artifactId>javax.servlet-api</artifactId>
			<scope>provided</scope>
		</dependency>
		<!--jaxb-impl is included due to jboss failing to load content wrappers RE-421-->
		<dependency>
			<groupId>com.sun.xml.bind</groupId>
			<artifactId>jaxb-impl</artifactId>
		</dependency>
		<dependency>
			<groupId>com.elasticpath</groupId>
			<artifactId>ep-test-application</artifactId>
		</dependency>

		<dependency>
			<groupId>com.elasticpath</groupId>
			<artifactId>ep-health-monitoring</artifactId>
		</dependency>
		<dependency>
			<groupId>com.elasticpath</groupId>
			<artifactId>ep-health-monitoring-plugin-core</artifactId>
		</dependency>
		<dependency>
			<groupId>com.elasticpath</groupId>
			<artifactId>ep-health-monitoring-plugin-jms</artifactId>
		</dependency>

	</dependencies>

	<build>
		<pluginManagement>
			<plugins>
				<plugin>
					<groupId>org.apache.tomcat.maven</groupId>
					<artifactId>tomcat8-maven-plugin</artifactId>
					<configuration>
						<path>${ep.datasync.context}</path>
						<port>${ep.datasync.tomcat.port.http}</port>
						<contextFile>${project.build.directory}/${project.build.finalName}/META-INF/context.xml</contextFile>

						<systemProperties>
							<ep.tomcat.maxcachesize>${ep.tomcat.maxcachesize}</ep.tomcat.maxcachesize>
							<epdb.validation.query>${epdb.validation.query}</epdb.validation.query>
							<epdb.username>${epdb.username}</epdb.username>
							<epdb.password>${epdb.password}</epdb.password>
							<epdb.jdbc.driver>${epdb.jdbc.driver}</epdb.jdbc.driver>
							<epdb.url>${epdb.url}</epdb.url>
							<epdb.data.source.factory>${epdb.data.source.factory}</epdb.data.source.factory>
							<epdb.data.source.type>${epdb.data.source.type}</epdb.data.source.type>
							<ep.datasync.port.http>${ep.datasync.tomcat.port.http}</ep.datasync.port.http>
							<ep.datasync.port.https>${ep.datasync.tomcat.port.https}</ep.datasync.port.https>
							<epdb.synctarget.validation.query>${epdb.synctarget.validation.query}</epdb.synctarget.validation.query>
							<epdb.synctarget.removeabandonedtimeout>${epdb.synctarget.removeabandonedtimeout}</epdb.synctarget.removeabandonedtimeout>
							<epdb.synctarget.username>${epdb.synctarget.username}</epdb.synctarget.username>
							<epdb.synctarget.password>${epdb.synctarget.password}</epdb.synctarget.password>
							<epdb.synctarget.jdbc.driver>${epdb.synctarget.jdbc.driver}</epdb.synctarget.jdbc.driver>
							<epdb.synctarget.url>${epdb.synctarget.url}</epdb.synctarget.url>
							<ep.synctarget.jms.url>${ep.synctarget.jms.url}</ep.synctarget.jms.url>
							<ep.jms.type>${ep.jms.type}</ep.jms.type>
							<ep.jms.factory>${ep.jms.factory}</ep.jms.factory>
							<ep.jms.url>${ep.jms.url}</ep.jms.url>
							<ep.changesets.enabled>${ep.changesets.enabled}</ep.changesets.enabled>
						</systemProperties>
					</configuration>


					<dependencies>
						<dependency>
							<groupId>org.apache.activemq</groupId>
							<artifactId>activemq-all</artifactId>
							<version>${org.apache.activemq.version}</version>
						</dependency>
						<dependency>
							<groupId>org.apache.camel</groupId>
							<artifactId>camel-jms</artifactId>
							<version>${org.apache.camel.version}</version>
						</dependency>
						<dependency>
							<groupId>org.apache.camel</groupId>
							<artifactId>camel-core</artifactId>
							<version>${org.apache.camel.version}</version>
						</dependency>
						<dependency>
							<groupId>commons-pool</groupId>
							<artifactId>commons-pool</artifactId>
							<version>${commons.pool.version}</version>
						</dependency>
						<dependency>
							<groupId>org.apache.commons</groupId>
							<artifactId>commons-pool2</artifactId>
							<version>${org.apache.commons.pool2.version}</version>
						</dependency>
						<!-- Include JDBC driver dependency -->
						<dependency>
							<groupId>${epdb.maven.groupId}</groupId>
							<artifactId>${epdb.maven.artifactId}</artifactId>
							<version>${epdb.maven.version}</version>
						</dependency>
					</dependencies>
				</plugin>
				<plugin>
					<artifactId>maven-war-plugin</artifactId>
					<configuration>
						<escapeString>\</escapeString>
						<webResources>
							<resource>
								<directory>src/main/webapp</directory>
								<filtering>true</filtering>
								<includes>
									<include>**/log4j.properties</include>
								</includes>
							</resource>
							<resource>
								<directory>src/main/webapp</directory>
								<filtering>false</filtering>
								<excludes>
									<exclude>**/log4j.properties</exclude>
								</excludes>
							</resource>
						</webResources>
					</configuration>
				</plugin>

				<plugin>
					<groupId>com.google.code.maven-replacer-plugin</groupId>
					<artifactId>replacer</artifactId>
					<version>${maven-replacer-plugin.version}</version>
				</plugin>
			</plugins>
		</pluginManagement>
	</build>

	<profiles>
		<profile>
			<id>with-tomcat-war</id>
			<activation>
				<activeByDefault>true</activeByDefault>
			</activation>
			<properties>
				<ep.datasync.port.http>${ep.datasync.tomcat.port.http}</ep.datasync.port.http>
				<ep.datasync.port.https>${ep.datasync.tomcat.port.https}</ep.datasync.port.https>
			</properties>
			<build>
				<plugins>
					<plugin>
						<artifactId>maven-resources-plugin</artifactId>
						<executions>
							<execution>
								<id>create-tomcat-context-xml</id>
								<phase>process-resources</phase>
								<goals>
									<goal>copy-resources</goal>
								</goals>
								<configuration>
									<resources>
										<resource>
											<directory>src/main/webapp/META-INF</directory>
											<filtering>true</filtering>
										</resource>
									</resources>
									<outputDirectory>${project.build.directory}/META-INF</outputDirectory>
								</configuration>
							</execution>
						</executions>
					</plugin>
				</plugins>
			</build>
		</profile>
	</profiles>

</project>