package com.elasticpath.selenium.navigations;

import org.openqa.selenium.WebDriver;

/**
 * Configuration Page.
 */
public class Configuration extends AbstractNavigation {

	/**
	 * Constructor.
	 *
	 * @param driver WebDriver which drives this page.
	 */
	public Configuration(final WebDriver driver) {
		super(driver);
	}
}
